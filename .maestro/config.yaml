# Only the numbered flows are entry points. Everything under subflows/ is called
# by them, and would otherwise be run on its own against no particular state.
#
# Two digits, not `0*`: that stopped matching at flow 10 and quietly ran nine
# flows while reporting them all green.
flows:
  - '[0-9][0-9]-*.yaml'
